// Package version pins the single version string surfaced by the CLI,
// the audit log, and the smoke test. It must match go.mod's header and
// CHANGELOG.md.
package version

// Version is the current certclerk release.
const Version = "0.1.0"
